build_id: v6.0.0
data_manifest_hash: placeholder_manifest_hash
calibration_hash: placeholder_calibration_hash
model_artifact_id: shadow_model_v1
shadow_model:
  start_date: 2024-01-01
  end_date: 2024-01-07
  evaluation: canary
comparison:
  metrics:
    sharpe_delta: 0.12
    turnover_delta: -0.03
    slippage_delta_bps: -1.5
# OrderFlow V6 发布签名（示例占位）
release_id: OFV6-YYYYMMDD-HHMM
created_at_utc: 2025-01-01T00:00:00Z
owner: devops
artifacts:
  data_manifest_hash: null  # 填写来自 results/manifest_hash.txt 的 SHA256
  calibration_hash: null    # 填写来自 calibration_profile.json 的签名
  model_artifact_id: null   # 训练产出模型的唯一 ID
  shadow_model_id: null     # 金丝雀影子模型（可选）
validation:
  qc_summary: results/qc_summary.md
  merge_and_calibration_report: results/merge_and_calibration_report.md
  validator_report: validation/out/report.md
execution:
  switch_policy: execution/switch_policy.yaml
  canary_window_days: 7
  observation_period: ">=1w"
external_distribution:
  partners: []
  notes: "共享前需确认脱敏与 NDA 签署"
ci_gates:
  - check: compliance_presence
    description: "COMPLIANCE.md / docs/traceability.md / releases/release.yml 必须存在且包含关键字"
  - check: signature_consistency
    description: "data_manifest_hash & calibration_hash 必须与结果文件一致"
